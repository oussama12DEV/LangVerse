import 'package:flutter/material.dart';

class OpponentSearchPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Search for Opponent'),
      ),
      body: Center(
        child: Text('Opponent search page placeholder'),
      ),
    );
  }
}
